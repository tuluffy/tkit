import * as React from 'react';

export interface ISFCNavigatorProps {}
type Props = ISFCNavigatorProps;

export const SFCNavigator: React.SFC<Props> = (props: Props) => {
  return <div className="common-presenter" />;
};

export default SFCNavigator;
