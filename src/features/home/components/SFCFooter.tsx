import * as React from 'react';

export interface ISFCFooterProps {}
type Props = ISFCFooterProps;

export const SFCFooter: React.SFC<Props> = (props: Props) => {
  return <div className="common-presenter" />;
};

export default SFCFooter;
